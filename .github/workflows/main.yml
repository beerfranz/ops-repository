on:
  push:
    branches: init

jobs:
  dockerhub:
    runs-on: ubuntu-latest
    name: build docker images and push them to dockerhub
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      # - name: Login to Docker Hub
      #   uses: docker/login-action@v2
      #   with:
      #     username: ${{ secrets.DOCKERHUB_USERNAME }}
      #     password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v3
        with:
          context: "api"
          file: {context}/Dockerfile.prod
          push: false
          tags: ops-repository:prod-local
          # target: app_php

      - name: Build and push caddy
        uses: docker/build-push-action@v3
        with:
          file: {context}/Dockerfile.api.alpine.caddy
          push: false
          tags: ops-repository:prod-local-caddy

      - name: Debug
        run: |
          docker images

      # - name: Run Buildx
      #   run: |
      #     docker buildx build \
      #       --platform linux/amd64 \
      #       --output "type=image,push=true" \
      #       --file ./Dockerfile \
      #       -t worshiptheuseless/nginx:latest .

      # -t worshiptheuseless/nginx:${{ steps.set_version.outputs.tag }} \

      # - name: Logout of Amazon ECR
      #   if: always()
      #   run: docker logout ${{ steps.login-ecr.outputs.registry }}
